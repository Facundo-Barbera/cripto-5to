services:
  web:
    build: .
    ports:
      - "5050:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - ./templates:/app/templates
      - ./static:/app/static
      - ./analyzer:/app/analyzer
      - ./app.py:/app/app.py
    restart: unless-stopped
